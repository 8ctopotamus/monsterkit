<div class="text-center">
    <h2>{{name}}</h2>
    <p>{{description}}</p>
    <p>Created by {{user.name}}</p>
    <p>
        <span for="img" aria-label="monster">🦖</span>
    </p>

    <p>
        <button class="btn btn-primary">Create Monster</button>
    </p>
</div>